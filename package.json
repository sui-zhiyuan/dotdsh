{
  "name": "dotdsh",
  "version": "0.1.0",
  "private": true,
  "description": "My dsh (DeepSeek Harness) plugin store: one plugin package per node_src/ directory, rows in the root cordis.patch.yml, wired into a profile by dotdsh_dev",
  "scripts": {
    "dev": "uv run python -m dotdsh_dev",
    "build": "pnpm -r build",
    "publish": "pnpm -r publish --access public",
    "book": "mdbook build"
  }
}
