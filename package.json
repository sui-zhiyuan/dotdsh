{
  "name": "dotdsh",
  "version": "0.1.0",
  "private": true,
  "description": "My dsh (DeepSeek Harness) plugin repo: the plugin packages under node_src/ plus the @dsh-external/dotdsh bundle that carries their patch rows; `dev_apply` builds and link-installs both",
  "scripts": {
    "apply": "uv run python -m dev_apply",
    "build": "pnpm -r build",
    "book": "mdbook build",
    "test": "pnpm -r test",
    "release": "pnpm -r publish --access public"
  }
}
