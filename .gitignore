# Build artifacts: all project output goes into target/<language>/
target/

# Dependencies and runtime artifacts
node_modules/
# Package build output (compiled from src/*.ts; rebuilt by sync_home.py / prepublishOnly)
node_src/**/lib/
*.log
.DS_Store
