[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dotdsh-dev"
version = "0.1.0"
description = "dotdsh dev-loop sync: link-install node_src plugins into a profile and copy the repo patch layer to its user layer"
requires-python = ">=3.11"
