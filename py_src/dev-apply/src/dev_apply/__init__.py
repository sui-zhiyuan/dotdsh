"""dev_apply: the dotdsh dev-loop CLI — build the plugin packages, link-install
them into a dsh profile, and remind you to restart dsh.

Run it as `uv run python -m dev_apply`; `--help` lists every flag.
"""
